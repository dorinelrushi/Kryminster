/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow external images from Unsplash (add more domains as needed)
  images: {
    domains: ['images.unsplash.com'],
  },
  /* config options here */
};

export default nextConfig;
